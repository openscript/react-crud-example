import { Session, initialSession } from "../models/Session";

export const sessionReducer = (state: Session = initialSession, action: any) => {
    /*switch (action.type) {
      default:
        return state;
    }*/
    return state;
  }